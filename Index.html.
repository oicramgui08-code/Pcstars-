
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PCSTARS | Hardwares e Equipamentos Gamer</title>
    <style>
        /* Paleta de Cores Premium Dark (Estilo Gamer/Cyberpunk) */
        :root {
            --bg-principal: #0a0a0c;
            --bg-card: #141419;
            --texto-principal: #ffffff;
            --texto-secundario: #a0a0b0;
            --cor-vermelho: #ff2a2a;   /* Vermelho Neon */
            --cor-amarelo: #ffcc00;    /* Amarelo Cyber */
            --borda: #22222a;
            --borda-hover: #3a3a4a;
        }

        * { 
            margin: 0; 
            padding: 0; 
            box-sizing: border-box; 
            font-family: 'Segoe UI', system-ui, sans-serif; 
        }

        body { 
            background-color: var(--bg-principal); 
            color: var(--texto-principal); 
            padding-bottom: 60px; 
        }

        /* --- NAVBAR --- */
        .navbar { 
            background-color: #000000; 
            padding: 20px; 
            position: sticky; 
            top: 0; 
            z-index: 1000; 
            border-bottom: 2px solid var(--cor-vermelho);
            box-shadow: 0 4px 20px rgba(255, 42, 42, 0.15);
        }
        .navbar-container { 
            max-width: 1200px; 
            margin: 0 auto; 
            display: flex; 
            align-items: center; 
            justify-content: space-between; 
            gap: 20px; 
        }
        
        /* Logo PCSTARS */
        .brand { 
            font-size: 1.8rem; 
            font-weight: 900; 
            letter-spacing: 1px; 
            color: var(--texto-principal);
            text-transform: uppercase;
        }
        .brand span { 
            color: var(--cor-vermelho); 
            text-shadow: 0 0 10px rgba(255, 42, 42, 0.6);
        }
        .brand label {
            color: var(--cor-amarelo);
            text-shadow: 0 0 10px rgba(255, 204, 0, 0.6);
            cursor: pointer;
        }

        /* Barra de Busca */
        .search-box { 
            flex-grow: 1; 
            max-width: 500px; 
            position: relative; 
        }
        .search-box input { 
            width: 100%; 
            padding: 12px 45px 12px 20px; 
            border-radius: 8px; 
            border: 1px solid var(--borda); 
            outline: none; 
            font-size: 0.95rem; 
            background: #181820; 
            color: #fff; 
            transition: all 0.3s;
        }
        .search-box input:focus {
            border-color: var(--cor-amarelo);
            box-shadow: 0 0 10px rgba(255, 204, 0, 0.2);
        }
        .search-box .search-icon { 
            position: absolute; 
            right: 15px; 
            top: 50%; 
            transform: translateY(-50%); 
            font-size: 1.1rem; 
            color: var(--texto-secundario); 
        }

        /* --- HERO BANNER --- */
        .hero-banner { 
            max-width: 1200px; 
            margin: 40px auto 20px auto; 
            padding: 0 20px; 
        }
        .hero-content { 
            background: linear-gradient(135deg, #141419, #08080a); 
            border-radius: 16px; 
            padding: 40px; 
            display: flex; 
            align-items: center; 
            justify-content: space-between; 
            gap: 30px; 
            border: 1px solid var(--borda);
            position: relative;
            overflow: hidden;
        }
        .hero-content::before {
            content: '';
            position: absolute;
            top: 0; left: 0; width: 4px; height: 100%;
            background: linear-gradient(to bottom, var(--cor-vermelho), var(--cor-amarelo));
        }
        
        .hero-text { max-width: 600px; }
        .hero-badge { 
            background-color: rgba(255, 42, 42, 0.1); 
            color: var(--cor-vermelho); 
            padding: 6px 14px; 
            border-radius: 4px; 
            font-size: 0.8rem; 
            font-weight: 700; 
            text-transform: uppercase; 
            display: inline-block; 
            margin-bottom: 15px; 
            border: 1px solid rgba(255, 42, 42, 0.3);
        }
        .hero-text h2 { 
            font-size: 2.8rem; 
            font-weight: 800; 
            line-height: 1.2; 
            margin-bottom: 15px; 
            letter-spacing: -0.5px;
        }
        .hero-text h2 span {
            color: var(--cor-amarelo);
        }
        .hero-text p { 
            font-size: 1.1rem; 
            color: var(--texto-secundario); 
        }

        /* --- VITRINE DE PRODUTOS --- */
        .section-container { 
            max-width: 1200px; 
            margin: 40px auto; 
            padding: 0 20px; 
        }
        .section-title { 
            font-size: 1.8rem; 
            font-weight: 800; 
            margin-bottom: 30px; 
            display: flex; 
            align-items: center; 
            gap: 10px; 
            text-transform: uppercase;
            letter-spacing: 1px;
            border-left: 4px solid var(--cor-vermelho); 
            padding-left: 15px; 
        }
        .section-title span {
            color: var(--cor-amarelo);
        }
        
        .products-grid { 
            display: grid; 
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); 
            gap: 30px; 
        }
        
        /* Card do Produto */
        .product-card { 
            background: var(--bg-card); 
            border-radius: 12px; 
            border: 1px solid var(--borda); 
            display: flex; 
            flex-direction: column; 
            overflow: hidden; 
            position: relative; 
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .product-card:hover { 
            transform: translateY(-5px); 
            border-color: var(--borda-hover);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.5);
        }
        
        .number-badge { 
            position: absolute; 
            top: 15px; 
            left: 15px; 
            background: rgba(0,0,0,0.75); 
            color: var(--cor-amarelo); 
            width: 32px; 
            height: 32px; 
            border-radius: 6px; 
            display: flex; 
            align-items: center; 
            justify-content: center; 
            font-weight: 800; 
            font-size: 0.9rem; 
            z-index: 10; 
            border: 1px solid var(--cor-amarelo);
        }
        .tag-badge { 
            position: absolute; 
            top: 15px; 
            right: 15px; 
            background: var(--cor-vermelho); 
            color: white; 
            padding: 5px 12px; 
            border-radius: 4px; 
            font-size: 0.75rem; 
            font-weight: 700; 
            text-transform: uppercase;
            z-index: 10; 
            box-shadow: 0 0 10px rgba(255, 42, 42, 0.4);
        }

        /* Container da Imagem */
        .product-image { 
            width: 100%; 
            height: 240px; 
            background: #ffffff; 
            padding: 20px; 
            display: flex; 
            align-items: center; 
            justify-content: center; 
            position: relative; 
            overflow: hidden; 
        }
        .product-image img { 
            max-width: 100%; 
            max-height: 100%; 
            object-fit: contain; 
            cursor: zoom-in; 
        }

        /* Configurações do Carrossel */
        .carousel-container { width: 100%; height: 100%; display: flex; position: relative; }
        .carousel-track { display: flex; width: 100%; height: 100%; transition: transform 0.4s ease-in-out; }
        .carousel-slide { min-width: 100%; height: 100%; display: flex; align-items: center; justify-content: center; background: white; }
        
        .carousel-btn { 
            position: absolute; 
            top: 50%; 
            transform: translateY(-50%); 
            background: rgba(0, 0, 0, 0.7); 
            color: white; 
            border: 1px solid #333; 
            width: 32px; 
            height: 32px; 
            border-radius: 4px; 
            cursor: pointer; 
            font-weight: bold; 
            font-size: 14px; 
            z-index: 5; 
            display: flex; 
            align-items: center; 
            justify-content: center; 
        }
        .carousel-btn:hover { background: var(--cor-vermelho); border-color: var(--cor-vermelho); }
        .btn-prev { left: 8px; }
        .btn-next { right: 8px; }

        .carousel-dots { position: absolute; bottom: 8px; left: 50%; transform: translateX(-50%); display: flex; gap: 6px; z-index: 5; }
        .dot { width: 8px; height: 8px; background: rgba(0,0,0,0.2); border-radius: 50%; cursor: pointer; border: 1px solid rgba(0,0,0,0.1); }
        .dot.active { background: var(--cor-vermelho); width: 16px; border-radius: 4px; }

        /* Informações do Bloco de Texto */
        .product-info { 
            padding: 25px; 
            display: flex; 
            flex-direction: column; 
            flex-grow: 1; 
        }
        .product-info h3 { 
            font-size: 1.1rem; 
            font-weight: 700; 
            color: var(--texto-principal); 
            margin-bottom: 10px; 
            line-height: 1.4; 
            min-height: 44px;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }
        .product-desc { 
            font-size: 0.88rem; 
            color: var(--texto-secundario); 
            margin-bottom: 20px; 
            line-height: 1.5;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        /* Preço e Botão */
        .price-box { margin-top: auto; }
        .price-label { 
            font-size: 0.75rem; 
            color: var(--texto-secundario); 
            text-transform: uppercase; 
            font-weight: 600; 
            letter-spacing: 0.5px;
            margin-bottom: 2px;
        }
        .price-value { 
            font-size: 1.6rem; 
            font-weight: 800; 
            color: var(--cor-amarelo); 
            margin-bottom: 15px; 
        }

        .btn-action { 
            display: block; 
            width: 100%; 
            text-align: center; 
            background-color: transparent; 
            color: #ffffff; 
            text-decoration: none; 
            padding: 14px; 
            border-radius: 6px; 
            font-weight: 700; 
            font-size: 0.95rem; 
            border: 2px solid var(--cor-vermelho); 
            cursor: pointer; 
            transition: all 0.3s;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .btn-action:hover { 
            background-color: var(--cor-vermelho); 
            box-shadow: 0 0 15px rgba(255, 42, 42, 0.4);
        }

        /* --- MODAL ZOOM --- */
        .image-modal { 
            position: fixed; 
            top:0; left:0; width:100%; height:100%; 
            background: rgba(0,0,0,0.95); 
            display:none; 
            justify-content:center; 
            align-items:center; 
            z-index: 2000; 
            padding: 20px; 
            opacity:0; 
            transition: opacity 0.3s ease; 
        }
        .image-modal.active { display:flex; opacity:1; }
        .image-modal-content { 
            max-width: 90%; 
            max-height: 85vh; 
            object-fit: contain; 
            border-radius: 4px; 
            border: 1px solid #333;
            transform: scale(0.9); 
            transition: transform 0.3s ease; 
        }
        .image-modal.active .image-modal-content { transform: scale(1); }
        .image-modal-close { 
            position: absolute; 
            top: 20px; 
            right: 25px; 
            color: #fff; 
            font-size: 35px; 
            font-weight: bold; 
            cursor: pointer; 
        }
        .image-modal-close:hover { color: var(--cor-vermelho); }

        .no-results { 
            display: none; 
            text-align: center; 
            padding: 60px; 
            font-size: 1.2rem; 
            color: var(--texto-secundario); 
            grid-column: 1 / -1; 
        }

        /* --- RESPONSIVIDADE --- */
        @media (max-width: 992px) {
            .hero-content { padding: 30px; text-align: center; justify-content: center; }
            .hero-text h2 { font-size: 2.2rem; }
        }
        @media (max-width: 600px) {
            .navbar { padding: 15px; }
            .brand { font-size: 1.4rem; }
            .products-grid { grid-template-columns: 1fr; gap: 20px; }
            .hero-text h2 { font-size: 1.8rem; }
        }
    </style>
</head>
<body>

    <div class="image-modal" id="imageModal">
        <span class="image-modal-close" id="modalClose">&times;</span>
        <img class="image-modal-content" id="modalImg" alt="Foto Ampliada">
    </div>

    <div class="navbar">
        <div class="navbar-container">
            <div class="brand">PC<span>STARS</span><label>★</label></div>
            <div class="search-box">
                <input type="text" id="searchInput" placeholder="Buscar hardware, monitores ou acessórios...">
                <span class="search-icon">🔍</span>
            </div>
        </div>
    </div>

    <div class="hero-banner">
        <div class="hero-content">
            <div class="hero-text">
                <span class="hero-badge">⚡ Setup Atualizado</span>
                <h2>Suba de nível com a <span>PCSTARS</span></h2>
                <p>A melhor seleção de hardware de alta performance, monitores gamer e periféricos com preços imbatíveis.</p>
            </div>
        </div>
    </div>

    <div class="section-container">
        <div class="section-title">Catálogo <span>Gamer</span></div>
        
        <div class="products-grid" id="productsGrid">

            <div class="product-card" data-name="monitor gamer computador pc tela hd led hz">
                <div class="number-badge">1</div>
                <div class="tag-badge">Top 1</div>
                <div class="product-image">
                    <div class="carousel-container">
                        <button class="carousel-btn btn-prev">&lt;</button>
                        <div class="carousel-track">
                            <div class="carousel-slide"><img src="https://i.ibb.co/RkDw6V50/image.jpg" alt="Monitor Gamer 1" class="zoomable-img"></div>
                            <div class="carousel-slide"><img src="https://i.ibb.co/9kzXybn0/image.jpg" alt="Monitor Gamer 2" class="zoomable-img"></div>
                            <div class="carousel-slide"><img src="https://i.ibb.co/N6BygGPb/image.jpg" alt="Monitor Gamer 3" class="zoomable-img"></div>
                        </div>
                        <button class="carousel-btn btn-next">&gt;</button>
                        <div class="carousel-dots">
                            <span class="dot active"></span>
                            <span class="dot"></span>
                            <span class="dot"></span>
                        </div>
                    </div>
                </div>
                <div class="product-info">
                    <h3>Monitor Gamer Profissional de Alta Performance</h3>
                    <p class="product-desc">Taxa de atualização ultra veloz e tempo de resposta mínimo. Imersão completa e movimentos fluidos para seus jogos competitivos.</p>
                    <div class="price-box">
                        <div class="price-label">Por apenas</div>
                        <div class="price-value">R$ 462,00</div>
                        <a href="https://meli.la/1vUo3QH" target="_blank" class="btn-action">Ver Oferta</a>
                    </div>
                </div>
            </div>

            <div class="product-card" data-name="fonte generica alimentacao pc desktop computador watts">
                <div class="number-badge">2</div>
                <div class="product-image">
                    <div class="carousel-container">
                        <button class="carousel-btn btn-prev">&lt;</button>
                        <div class="carousel-track">
                            <div class="carousel-slide"><img src="https://i.ibb.co/8DHG13HK/image.jpg" alt="Fonte 1" class="zoomable-img"></div>
                            <div class="carousel-slide"><img src="https://i.ibb.co/0yS3GN2P/image.jpg" alt="Fonte 2" class="zoomable-img"></div>
                        </div>
                        <button class="carousel-btn btn-next">&gt;</button>
                        <div class="carousel-dots">
                            <span class="dot active"></span>
                            <span class="dot"></span>
                        </div>
                    </div>
                </div>
                <div class="product-info">
                    <h3>Fonte de Alimentação ATX Estabilizada para PC</h3>
                    <p class="product-desc">Componente essencial para desktops convencionais de escritório. Distribuição de energia estável com excelente custo-benefício.</p>
                    <div class="price-box">
                        <div class="price-label">Por apenas</div>
                        <div class="price-value">R$ 28,00</div>
                        <a href="https://meli.la/2r4Btyw" target="_blank" class="btn-action">Ver Oferta</a>
                    </div>
                </div>
            </div>

            <div class="product-card" data-name="controle bluetooth gamepad sem fio celular pc console joystick">
                <div class="number-badge">3</div>
                <div class="product-image">
                    <div class="carousel-container">
                        <button class="carousel-btn btn-prev">&lt;</button>
                        <div class="carousel-track">
                            <div class="carousel-slide"><img src="https://i.ibb.co/hRyPcKzy/image.jpg" alt="Controle 1" class="zoomable-img"></div>
                            <div class="carousel-slide"><img src="https://i.ibb.co/fzmZK6zF/image.jpg" alt="Controle 2" class="zoomable-img"></div>
                        </div>
                        <button class="carousel-btn btn-next">&gt;</button>
                        <div class="carousel-dots">
                            <span class="dot active"></span>
                            <span class="dot"></span>
                        </div>
                    </div>
                </div>
                <div class="product-info">
                    <h3>Controle Bluetooth Sem Fio Inteligente (Gamepad)</h3>
                    <p class="product-desc">Design ergonômico com alta precisão nos analógicos. Perfeito para gameplays em smartphones, PC e consoles sem atraso de resposta.</p>
                    <div class="price-box">
                        <div class="price-label">Por apenas</div>
                        <div class="price-value">R$ 54,00</div>
                        <a href="https://meli.la/1u64ruW" target="_blank" class="btn-action">Ver Oferta</a>
                    </div>
                </div>
            </div>

            <div class="product-card" data-name="ring light iluminador led maquiagem gravacao">
                <div class="number-badge">4</div>
                <div class="product-image">
                    <img src="https://i.ibb.co/vvYQ4kPM/download.webp" alt="Ring light" class="zoomable-img">
                </div>
                <div class="product-info">
                    <h3>Ring Light Iluminador de LED - Edição Especial</h3>
                    <p class="product-desc">Iluminação perfeita e profissional para suas transmissões de jogos, vídeos e fotos. Tripé ajustável e troca de tons
